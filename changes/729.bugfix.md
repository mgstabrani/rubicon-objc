Comparing an `NSString` to a non-string object with `!=` no longer raises a `DeprecationWarning`, and multiplying an `NSString` by a non-integer now raises a `TypeError` describing the unsupported operands, rather than one about integer interpretation.
